plugins {
    id "com.gradle.enterprise" version "3.12.3"
}

gradleEnterprise {
    server = "https://ge.solutions-team.gradle.com"
    buildScan {
        publishAlways()
    }
}

rootProject.name = 'gradle-deps'
include('app', 'list', 'utilities')
